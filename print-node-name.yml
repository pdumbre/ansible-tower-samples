---
- name: Display all host variables and facts for each host
  hosts: all
  gather_facts: true  # Ensure facts are gathered if needed
  tasks:
    - name: Print all variables for the current host
      ansible.builtin.debug:
        var: hostvars[inventory_hostname]
      # The output will appear as a JSON structure in the AWX job log
