# User management 

- name: Get current IMM users via Redfish
  community.general.redfish_info:
    category: Accounts
    command: ListUsers
    baseuri: "{{ bmc_ip }}"
    username: "{{ redfish_user}}"
    password: "{{ redfish_password }}"
  register: current_users
  failed_when: false

- name: Debug current users
  ansible.builtin.debug:
    var: current_users
   
