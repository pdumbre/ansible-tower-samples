
- name: Discover resources
  include_tasks: discovery.yml
      
- name: Probe job queue endpoint availability
  uri:
    url: "{{ bmc_manager_url }}/Jobs"
    method: GET
    user: "{{ bmc_user }}"
    password: "{{ bmc_password }}"
    validate_certs: no
    force_basic_auth: yes
    return_content: yes
    status_code: [200, 404]
  register: probe_job_queue
  failed_when: false
  when: not (ru_number | int == 23)

- name: Set BIOS apply method flag
  set_fact:
    use_bios_jobqueue: "{{ probe_job_queue.status | default(404) == 200 }}"
  when: not (ru_number | int == 23)

- name: Detect model and resolve configuration flags
  include_tasks: bios_detect.yml
  when: not (ru_number | int == 23)

#- name: Configure PXE settings and reboot cycles
 # include_tasks: bios_pxe_configure.yml
 # when: not (ru_number | int == 23)

#- name: Apply BIOS settings via job queue
 # include_tasks: bios_jobqueue.yml
  #when:
   # - not (ru_number | int == 23)
    #- use_bios_jobqueue | bool

#- name: Resolve and set boot order
 # include_tasks: bios_boot_order.yml
  #when: not (ru_number | int == 23)

